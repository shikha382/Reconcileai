# Tested, reproducible Python 3.12 environment.  Keep these direct
# dependencies pinned: broad lower bounds previously allowed a future
# FastAPI release to change router internals and break two repository tests.
sqlalchemy==2.0.35
pydantic==2.12.5
pytest==8.0.0
rapidfuzz==3.14.3
fastapi==0.136.3
uvicorn==0.40.0
httpx==0.28.1

# Transitive pins captured from the verified Python 3.12 environment so a
# future resolver run cannot silently change the tested stack.
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.0
certifi==2026.7.22
click==8.5.0
h11==0.16.0
httpcore==1.0.9
idna==3.19
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
pydantic_core==2.41.5
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
